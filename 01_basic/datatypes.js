"use strict" //treat all code as newer version of js

// alert(3+3) // will not work because we are using node, not browser

console.log(3+3)

console.log('vishal')

//datatypes example
let name = "vishal" // string
let age = 18 // age
let isLogin = true  //boolean

//datatypes

// number => 2 to power 53,
// bigint,
// string => "",
// boolean => true/false,
// null => standalone value => empty

// undefined => 
// symbol => for unique   (Uniqueness: Every symbol is guaranteed to be unique, even if two symbols have the same description.
//      Non-enumerable: Symbols are not included in for...in loops or Object.keys() iterations, making them useful for defining "hidden" properties.
//      Use case: Symbols are often used when you want to create a property that shouldn't clash with any other property names, especially when you're dealing with objects or libraries.)


// object


//for get datatype we use typeof method
 console.log(typeof age)

 console.log(typeof null)
 console.log(typeof undefined)


